# SPI Stack - Bare profile
#
# Deliberately empty. GitOps remains activated and healthy, but Flux has no
# workloads to converge. The CLI bootstrap seeds namespaces, secrets, the
# osdu-config ConfigMap, and the Workload Identity ServiceAccount.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
